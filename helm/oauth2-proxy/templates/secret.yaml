apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-credentials
  namespace: oauth2-proxy
type: Opaque
stringData:
  client-id: "${OAUTH2_PROXY_GH_CLIENT_ID}"
  client-secret: "${OAUTH2_PROXY_GH_CLIENT_SECRET}"
  cookie-secret: "${OAUTH2_PROXY_COOKIE_SECRET}"
